version: "3.8"
services:

  surrealdb:
    image: surrealdb/surrealdb:latest
    restart: unless-stopped
    ports:
      - 8000:8000
    networks:
      - my-garden

networks:
  my-garden:
    driver: bridge
